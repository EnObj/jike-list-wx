<!--此处增加hidden防止切换时-->
<view style="height:100%;" wx:if="{{channel.code==programList.channelCode}}">
  <program-list-a program-list="{{programList}}" wx:if="{{channel.listStyle=='a'}}"/>
  <program-list-b program-list="{{programList}}" wx:if="{{channel.listStyle=='b'}}"/>
</view>