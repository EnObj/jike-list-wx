<!-- <view class="channel-logo">
  <view class="logo">
    <image style="width:60px;height:40px;" mode="aspectFit" src="{{channel.logo}}"></image>
  </view>
</view> -->

<view class="channel-date">
  <date-selector loc-date="{{options.date}}" bindswitchdate="switchDate" />
</view>

<view>
  <view class="programs" wx:if="{{options.channel && currentDate}}">
    <program-list channel="{{options.channel}}" date="{{currentDate}}" list="{{programList}}" />
  </view>
</view>