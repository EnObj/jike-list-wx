
<search-inp keyword="{{options.keyword}}" bindkeywordchange="search" bindfocus="focus"/>

<view wx:if="{{keyword}}">
  <view>
    <search-day keyword="{{keyword}}" mode="card"/>
  </view>
  <view>
    <search-year keyword="{{keyword}}" mode="card"/>
  </view>
</view>
